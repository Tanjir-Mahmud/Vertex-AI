import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "VERITAS AI — Multi-Modal Truth Scanner",
  description:
    "AI-Powered Digital Trust & Deepfake Detection Dashboard. Verify claims, detect AI-generated text and images, and uncover misinformation in real-time.",
  keywords: [
    "fact-check",
    "deepfake detection",
    "AI detection",
    "trust score",
    "misinformation",
    "veritas",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
